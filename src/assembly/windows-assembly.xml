<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
                              http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    
    <id>windows</id>
    <formats>
        <format>zip</format>
        <format>dir</format>
    </formats>
    
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>${project.artifactId}</baseDirectory>
    
    <fileSets>
        <!-- Скрипт запуска для Windows -->
        <fileSet>
            <directory>src/dist/bin</directory>
            <outputDirectory>bin</outputDirectory>
            <includes>
                <include>run.bat</include>
            </includes>
        </fileSet>
        
        <!-- Пример конфигурации -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>config</outputDirectory>
            <includes>
                <include>.env.example</include>
            </includes>
        </fileSet>
        
        <!-- Документация -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory></outputDirectory>
            <includes>
                <include>README.md</include>
                <include>CHANGELOG.md</include>
            </includes>
        </fileSet>
    </fileSets>
    
    <files>
        <!-- JAR файл приложения -->
        <file>
            <source>${project.build.directory}/${project.build.finalName}.jar</source>
            <outputDirectory>app</outputDirectory>
            <destName>${project.artifactId}.jar</destName>
        </file>
    </files>
    
    <!-- 
    ПРИМЕЧАНИЕ: JRE не включается в этот assembly descriptor.
    Используйте build-dist.sh для создания полного дистрибутива с JRE.
    Этот assembly создает только структуру без JRE для случаев,
    когда JRE уже установлен на целевой машине.
    -->
    
</assembly>
